# CI-only requirements — excludes packages that need Windows/display:
#   xlwings (requires Excel COM / pywin32)
#   ttkbootstrap (requires Tk display server)
#
# Tests bypass xlwings via CSV fixtures (see tests/local/conftest.py).
# reportlab is pure-Python and required by tests/local/test_pdf_report.py
# (covers L1-27 PDF Structure Report); keep in sync with requirements.txt.

pandas==2.3.3
networkx==3.6.1
orjson==3.11.7
cryptography==46.0.4
reportlab==4.4.9
pytest>=8.0
